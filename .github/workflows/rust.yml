name: Rust

on:
  push:
    branches: [ "master", "latest" ]
  pull_request:
    branches: [ "latest" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  acme:
    env:
      CARGO_PKG_NAME: acme
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login
        run: cargo login ${{ secrets.CARGO_REGISTRY_TOKEN }}
      - name: Test
        run: cargo test -p $CARGO_PKG_NAME -v --color $CARGO_TERM_COLOR --all-features
      - name: Build and Push
        run: cargo publish -p $CARGO_PKG_NAME --all-features